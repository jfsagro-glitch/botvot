# 🚂 Руководство по деплою на Railway

## Быстрый старт

### 1. Подготовка репозитория

Ваш проект уже настроен для Railway:
- ✅ `railway.json` - конфигурация Railway
- ✅ `nixpacks.toml` - настройки сборки
- ✅ `Dockerfile` - альтернативная сборка через Docker
- ✅ `run_all_bots.py` - точка входа для запуска ботов
- ✅ `.railwayignore` - исключает ненужные файлы из деплоя

### 2. Настройка в Railway

#### Шаг 1: Создайте новый проект
1. Перейдите на https://railway.app
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий `botvot`

#### Шаг 2: Добавьте переменные окружения

В разделе **Variables** добавьте:

**Обязательные:**
```
SALES_BOT_TOKEN=ваш_токен_продающего_бота
COURSE_BOT_TOKEN=ваш_токен_курс_бота
```

**Рекомендуемые:**
```
ADMIN_CHAT_ID=ваш_chat_id
GENERAL_GROUP_ID=id_общей_группы
PREMIUM_GROUP_ID=id_премиум_группы
DATABASE_PATH=./data/course_platform.db
PAYMENT_PROVIDER=mock
```

#### Шаг 3: Настройте постоянное хранилище (опционально)

Для сохранения базы данных между перезапусками:
1. Перейдите в раздел **Settings**
2. Добавьте **Volume** (постоянное хранилище)
3. Примонтируйте его к `/app/data`

#### Шаг 4: Настройте домен (опционально)

1. Перейдите в раздел **Settings** → **Networking**
2. Нажмите "Generate Domain" для получения публичного URL
3. Или добавьте свой кастомный домен

### 3. Автоматический деплой

Railway автоматически деплоит при каждом push в ветку `main`:
```bash
git push origin main
```

### 4. Проверка работоспособности

После деплоя проверьте:

1. **Логи деплоя:**
   - Откройте раздел **Deployments**
   - Выберите последний деплой
   - Проверьте логи на ошибки

2. **Healthcheck:**
   - Должен показать статус "Healthy"
   - HTTP сервер отвечает на `/` и `/health`

3. **Тест ботов:**
   - Откройте Telegram
   - Напишите `/start` вашему боту
   - Бот должен ответить

## Структура проекта для Railway

```
BOTVOT/
├── bots/              # Код ботов (включено)
├── core/              # Ядро системы (включено)
├── services/          # Сервисы (включено)
├── utils/             # Утилиты (включено)
├── data/              # База данных (исключено из деплоя, создается автоматически)
├── Photo/             # Медиа файлы (исключено из деплоя - слишком большие)
├── scripts/           # Скрипты разработки (исключено)
├── railway.json       # Конфигурация Railway
├── nixpacks.toml      # Настройки сборки
├── Dockerfile         # Альтернативная сборка
├── requirements.txt   # Зависимости Python
└── run_all_bots.py    # Точка входа
```

## Переменные окружения

### Обязательные переменные

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `SALES_BOT_TOKEN` | Токен продающего бота от @BotFather | `123456789:ABC...` |
| `COURSE_BOT_TOKEN` | Токен курс-бота от @BotFather | `123456789:XYZ...` |

### Опциональные переменные

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `ADMIN_CHAT_ID` | ID чата администратора для обратной связи | `0` |
| `GENERAL_GROUP_ID` | ID общей группы | `""` |
| `PREMIUM_GROUP_ID` | ID премиум группы | `""` |
| `CURATOR_GROUP_ID` | ID группы кураторов | `""` |
| `DATABASE_PATH` | Путь к файлу базы данных | `./data/course_platform.db` |
| `PAYMENT_PROVIDER` | Провайдер платежей (`mock` или `yookassa`) | `mock` |
| `COURSE_DURATION_DAYS` | Длительность курса в днях | `30` |
| `LESSON_INTERVAL_HOURS` | Интервал между уроками в часах | `24` |

## Мониторинг и логи

### Просмотр логов

1. Откройте проект в Railway
2. Перейдите в раздел **Deployments**
3. Выберите активный деплой
4. Нажмите "View Logs"

### Метрики

Railway автоматически отслеживает:
- Использование CPU
- Использование памяти
- Сетевой трафик
- Количество запросов

### Healthcheck

Railway автоматически проверяет здоровье сервиса:
- Endpoint: `/` или `/health`
- Timeout: 300 секунд
- Interval: 30 секунд

## Решение проблем

### Проблема: Healthcheck не проходит

**Решение:**
1. Проверьте логи - должен быть запущен HTTP сервер
2. Убедитесь, что порт читается из переменной `PORT`
3. Проверьте, что сервер отвечает на `/` с кодом 200

### Проблема: Боты не запускаются

**Решение:**
1. Проверьте переменные окружения в Railway Variables
2. Убедитесь, что токены правильные (без кавычек, пробелов)
3. Проверьте логи - должна быть диагностика конфигурации

### Проблема: База данных не сохраняется

**Решение:**
1. Добавьте Volume (постоянное хранилище) в Railway
2. Примонтируйте его к `/app/data`
3. Убедитесь, что `DATABASE_PATH` указывает на примонтированный путь

### Проблема: Медленная сборка

**Решение:**
1. Проверьте `.railwayignore` - исключает ли большие файлы
2. Убедитесь, что медиа файлы не включены в деплой
3. Рассмотрите использование Dockerfile для оптимизации

## Обновление деплоя

После изменения кода:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Railway автоматически:
1. Обнаружит новый коммит
2. Запустит сборку
3. Развернет новую версию
4. Проверит healthcheck

## Откат к предыдущей версии

1. Откройте раздел **Deployments**
2. Найдите нужную версию
3. Нажмите "Redeploy"

## Удаление деплоя

1. Откройте настройки проекта
2. Перейдите в **Danger Zone**
3. Нажмите "Delete Project"

## Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
- [Railway Status](https://status.railway.app)

## Поддержка

Если возникли проблемы:
1. Проверьте логи деплоя
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию Railway
4. Обратитесь в поддержку Railway через Discord
