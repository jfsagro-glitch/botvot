"""
Система напоминаний от наставника.

Требования:
- Тон: мягкий, позитивный, чуть с юмором
- Эмодзи: преимущественно из согласованного набора
- Сообщение должно содержать само задание (или его краткий фрагмент)
"""

import random
from typing import List


SOFT_TEMPLATES = [
    "💬 Я тут на минутку. Давай сделаем маленький шаг по заданию — и будет 💙.",
    "💬 Мини-напоминание без паники: задание на сегодня всё ещё ждёт тебя. 💠",
    "💬 Если есть 2–3 минуты — этого уже достаточно, чтобы начать. ⏱️",
    "💬 Включаем режим «чуть-чуть, но регулярно». 💙",
    "💬 У задания настроение: «я буду коротким, если ты начнёшь». 🎲",
    "💬 Договор такой: ты делаешь один пункт, а остальное пусть догоняет потом. 💠",
    "💬 Никаких героических подвигов. Просто один честный шаг. 🤍",
    "💬 Если сегодня хочется 💤 — ок. Тогда сделай самый лёгкий кусочек задания. 💙",
]


def get_mentor_reminder_text(assignment_text: str) -> str:
    """
    Генерирует случайное напоминание о задании.
    
    Args:
        assignment_text: Текст задания
    
    Returns:
        Текст напоминания с заданием
    """
    reminder_template = random.choice(SOFT_TEMPLATES)

    # Keep message compact: show only first chunk of the task to avoid huge spam.
    task = (assignment_text or "").strip()
    if len(task) > 900:
        task = task[:900].rstrip() + "…"

    return (
        f"{reminder_template}\n\n"
        f"📘 <b>Задание</b>\n"
        f"{task}\n\n"
        f"☑️ Когда сделаешь — просто отправь ответ сюда (как обычно)."
    )


def get_mentor_emoji_sequence() -> List[str]:
    """
    Возвращает последовательность эмодзи наставника для анимации.
    
    Returns:
        Список эмодзи для последовательной отправки
    """
    # Single emoji to avoid spam; keep it consistent with the bot UI.
    return ["👨‍🏫"]
