# 🔧 Настройка переменных окружения в Railway

## ❗ КРИТИЧЕСКИ ВАЖНО: Переменные все еще не установлены!

По логам видно:
```
SALES_BOT_TOKEN=❌ не установлен
COURSE_BOT_TOKEN=❌ не установлен
```

## 📋 Пошаговая инструкция добавления переменных:

### Шаг 1: Откройте проект в Railway
1. Перейдите на https://railway.app
2. Войдите в свой аккаунт
3. Откройте проект "worker" или "рабочий"

### Шаг 2: Перейдите в раздел Variables
1. В левом меню найдите раздел **"Variables"** (или **"Переменные"**)
2. Нажмите на него

### Шаг 3: Добавьте переменные (ВАЖНО - правильный формат!)

**Нажмите кнопку "+ New Variable" или "Add Variable"**

#### Переменная 1:
- **Key (Ключ)**: `SALES_BOT_TOKEN`
- **Value (Значение)**: Вставьте токен бота (БЕЗ кавычек, БЕЗ пробелов!)
- ❌ НЕПРАВИЛЬНО: `"123456789:ABC..."`
- ❌ НЕПРАВИЛЬНО: ` 123456789:ABC... ` (с пробелами)
- ✅ ПРАВИЛЬНО: `123456789:ABC...`

#### Переменная 2:
- **Key (Ключ)**: `COURSE_BOT_TOKEN`
- **Value (Значение)**: Вставьте токен бота (БЕЗ кавычек, БЕЗ пробелов!)

### Шаг 4: Сохраните переменные
1. Нажмите **"Save"** или **"Add"**
2. Подождите 5-10 секунд
3. Railway автоматически перезапустит сервис

### Шаг 5: Проверьте логи
1. Перейдите в раздел **"Deployments"**
2. Выберите последний деплой
3. Откройте **"Deploy Logs"**
4. Должно появиться:
   ```
   SALES_BOT_TOKEN из os.environ: ✅ есть (длина: XX)
   COURSE_BOT_TOKEN из os.environ: ✅ есть (длина: XX)
   📋 Проверка конфигурации: SALES_BOT_TOKEN=✅ установлен, COURSE_BOT_TOKEN=✅ установлен
   ```

## 🔍 Как получить токены ботов:

### Метод 1: Через @BotFather в Telegram
1. Откройте Telegram
2. Найдите `@BotFather`
3. Отправьте `/mybots`
4. Выберите вашего бота (например, `@StartNowQ_bot`)
5. Нажмите **"API Token"**
6. Скопируйте токен (он выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Метод 2: Если бот уже создан, но токен потерян
1. Откройте `@BotFather`
2. Отправьте `/mybots`
3. Выберите бота
4. Нажмите **"API Token"**
5. Если токен показан - скопируйте его
6. Если нет - можете создать новый через **"Revoke Token"** → **"Generate New Token"**

## ⚠️ Частые ошибки:

### Ошибка 1: Добавлены кавычки
- ❌ `"123456789:ABC..."`
- ✅ `123456789:ABC...`

### Ошибка 2: Лишние пробелы
- ❌ ` 123456789:ABC... ` (пробелы в начале/конце)
- ✅ `123456789:ABC...`

### Ошибка 3: Неправильное имя переменной
- ❌ `SALES_BOT_TOKEN ` (пробел в конце)
- ❌ `sales_bot_token` (маленькие буквы)
- ❌ `SALES-BOT-TOKEN` (дефисы вместо подчеркиваний)
- ✅ `SALES_BOT_TOKEN` (точное совпадение)

### Ошибка 4: Переменные добавлены в неправильный сервис
- Убедитесь, что добавляете переменные в сервис **"worker"** (не в другой сервис проекта)

## 🔄 После добавления переменных:

Railway автоматически перезапустит сервис. Через 1-2 минуты:

1. ✅ Healthcheck должен пройти успешно
2. ✅ В логах должны появиться сообщения об инициализации ботов
3. ✅ Боты должны начать отвечать в Telegram

## 📞 Если переменные все еще не работают:

1. Проверьте, что они действительно сохранены в Railway Variables
2. Убедитесь, что нет опечаток в именах переменных
3. Проверьте логи - там будет подробная диагностика
4. Попробуйте удалить и заново добавить переменные
5. Убедитесь, что токены валидные (можно проверить через @BotFather)
