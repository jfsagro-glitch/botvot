# Развертывание ботов на сервере

Для того, чтобы боты работали постоянно, их нужно развернуть на облачном сервере.

## 🚀 Быстрый старт с Railway (Рекомендуется)

### Шаг 1: Регистрация

1. Зарегистрируйтесь на https://railway.app/
2. Войдите через GitHub

### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий `botvot`
4. Railway автоматически определит проект

### Шаг 3: Настройка переменных окружения

В настройках проекта (Settings → Variables) добавьте:

```
SALES_BOT_TOKEN=ваш_токен_продающего_бота
COURSE_BOT_TOKEN=ваш_токен_курс_бота
ADMIN_CHAT_ID=ваш_chat_id
GENERAL_GROUP_ID=id_общей_группы
PREMIUM_GROUP_ID=id_премиум_группы
PAYMENT_PROVIDER=yookassa
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
YOOKASSA_RETURN_URL=https://t.me/StartNowQ_bot
PAYMENT_CURRENCY=RUB
DATABASE_PATH=/tmp/course_platform.db
```

### Шаг 4: Запуск

Railway автоматически:
- Установит зависимости из `requirements.txt`
- Запустит `run_all_bots.py` (оба бота в одном процессе)
- Будет перезапускать при сбоях

### Шаг 5: Мониторинг

- Логи доступны в разделе "Deployments"
- Метрики в разделе "Metrics"
- При ошибках боты автоматически перезапускаются

## 🌐 Альтернативные платформы

### Render

1. Зарегистрируйтесь на https://render.com/
2. Создайте "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python run_all_bots.py`
5. Добавьте переменные окружения

### Fly.io

1. Установите flyctl: https://fly.io/docs/getting-started/installing-flyctl/
2. Создайте `Dockerfile` (см. ниже)
3. Выполните:
   ```bash
   fly launch
   fly secrets set SALES_BOT_TOKEN=...
   fly secrets set COURSE_BOT_TOKEN=...
   # и т.д.
   ```

### VPS (DigitalOcean, Hetzner, AWS)

1. Создайте сервер (Ubuntu 22.04)
2. Установите Python 3.11+
3. Клонируйте репозиторий
4. Установите зависимости
5. Настройте systemd service (см. `botvot.service` ниже)
6. Запустите: `sudo systemctl start botvot`

## 📝 Systemd Service (для VPS)

Создайте файл `/etc/systemd/system/botvot.service`:

```ini
[Unit]
Description=Telegram Course Platform Bots
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/botvot
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/bin/python3 run_all_bots.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable botvot
sudo systemctl start botvot
sudo systemctl status botvot
```

## 🔄 Обновление

При пуше в GitHub:
- Railway/Render: автоматическое обновление
- VPS: `git pull && sudo systemctl restart botvot`

## ⚠️ Важно

- Храните токены в секретах платформы, не в коде
- Используйте базу данных на диске (для VPS) или внешнюю (PostgreSQL)
- Настройте мониторинг и алерты
- Регулярно проверяйте логи

